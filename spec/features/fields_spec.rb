require 'spec_helper'

describe "Fields" do
  describe "GET /fields" do
    it "works! (now write some real specs)" do
      visit fields_path
      assert current_path == fields_path
    end
  end
end
