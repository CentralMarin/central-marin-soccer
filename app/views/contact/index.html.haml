%h1.grid_16= t('contact.heading')

- Contact.categories.each do |symbol, value|
  %div.grid_16.box
    %h2.alt= t('contact.' + symbol)
    %ul
      - @contacts_by_category[symbol.to_sym].each do |contact|
        %li
          %a{href: "##{contact.club_position}"}
            = "#{contact.club_position} - #{contact_name(contact.name)}"

= render partial: 'apply_position'

- Contact.categories.each do |symbol, value|
  %div.grid_16.box
    %h2.alt= t('contact.' + symbol)
    - @contacts_by_category[symbol.to_sym].each do |contact|
      = render partial: 'contact_info', :locals => {contact: contact }

= render partial: 'apply_position'
